//#include "TableController.h"

#include "TupleDeserializer.h"
#include <iostream>
#include "application/model/Table.h"


// todo почему нельзя
// std::tuple<int, char> t1 = std::tuple(8, 'a');
// table.add(t1);
// но можно
// std::tuple<const int, char> t1 = std::tuple(8, 'a');
// table.add(t1);
// но можно
// std::tuple<const int, char> t1 = std::tuple(8, 'a');
// table.add(std::move(t1));


int main() {
//    Models::Table<int, char> table;
//
//
//    table.emplaceAdd(1, '3');
//    table.emplaceAdd(2, '3');
//    table.emplaceAdd(3, '3');
//    table.emplaceAdd(4, '3');
//    table.emplaceAdd(5, '3');
//
//    std::tuple<int, char> t1 = std::tuple(8, 'a');
////    std::tuple<int, char> t2 = std::move(t1);
//
////    foo(t1);
//
//    table.add(std::move(t1));
//
//
////    table.post(t1);
//
//    for (auto & it : table)
//        std::cout << std::get<0>(it) << " " << std::get<1>(it) << std::endl;




    return 0;
}